services:
  app:
    build:
      context: .
      dockerfile: docker/frankenphp/Dockerfile
    image: ttlock/dashboard:local
    container_name: ttlock_dashboard_local
    network_mode: host
    env_file:
      - .env
    restart: unless-stopped
